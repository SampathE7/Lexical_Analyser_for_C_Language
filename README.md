# Lexical_Analyser_for_C_Language
This project implements a Lexical Analyzer (Tokenizer) for the C programming language. The lexical analyzer reads a C source file character by character and converts it into a sequence of tokens.
